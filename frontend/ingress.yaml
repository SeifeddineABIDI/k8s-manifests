apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: approbation-frontend-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: public
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: approbation-frontend
            port:
              number: 80